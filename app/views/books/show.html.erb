<h2>Details for book <%= @book.title %></h2>
<p>
  Author: <%= @book.author ? @book.author.name : "<unknown>" %>
</p>

<p>
  Published: <%= @book.publication_year %>
</p>

<p>
  Synopsis: <%= @book.synopsis %>
</p>

<!-- New show code to display genres -->
<p>Genres:</p>
<ul>
  <% @book.genres.each do |genre| %>
    <li>
      <%= genre.name %>
    </li>
  <% end %>
</ul>

<%= link_to "Edit this book", edit_book_path(@book), class: "hollow button" %>
